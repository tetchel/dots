#!/usr/bin/env bash

#default_ns="default"
default_ns="che"

# use default_ns unless one was passed
if [[ "$@" == *"-n "* || "$@" == *"--namespace "* || "$@" == *"--all-namespaces"* ]]; then
    kubectl $@
else
    # echo "$(basename $0): Using -n default"
    kubectl -n $default_ns $@
fi
