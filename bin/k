#!/usr/bin/env bash

# use 'default' namespace unless one was passed
if [[ "$@" == *"-n "* || "$@" == *"--namespace "* || "$@" == *"--all-namespaces"* ]]; then
    kubectl $@
else
    # echo "$(basename $0): Using -n default"
    kubectl -n default $@
fi
